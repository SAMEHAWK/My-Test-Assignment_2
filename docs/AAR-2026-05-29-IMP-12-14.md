# AAR — IMP-12～14 第二轮改进

## 元信息

| 项 | 内容 |
|----|------|
| 日期 | 2026-05-29 |
| 范围 | IMP-12 ResourceInventory、IMP-13 Awake 缓存、IMP-14 BuildingProductionView |
| 参与者 | AI + 人工 |
| 关联文档 | [架构设计.md](架构设计.md) v1.7→v1.8、[项目改进计划.md](项目改进计划.md) v1.3 |

---

## 1. 目标与结果

**计划目标**：消除库存 DRY 违规、Singleton 热路径访问、停产仅 TMP 无玩法反馈（公司评审反馈 G12～G14）。

**实际结果**：

- IMP-12：`Core.ResourceInventory` + Warehouse/Backpack 适配器；`ResourceInventoryTests` 集中测 Domain
- IMP-13：5 类 Awake 缓存；`-Strict` singleton 扫描 exit 0
- IMP-14：`BuildingProductionView` + N1 Producer Prefab（fileID 已修正）
- 程序集：`ProductionChain.asmdef` + Input 迁入 `Scripts/Input/`；`EditModeTests` → `ProductionChain`

**是否达成**：**部分** — 代码与文档已完成；EditMode batchmode 因 Unity 编辑器占用未跑；IMP-06 Player Prefab、IMP-05 Git 待阶段 G

---

## 2. 做得好的

- L0 概念检查驱动 IMP-12（四维 4 YES → 抽取 Domain）
- L1 测试纪律：Domain 测一次，适配器薄测
- 程序集根因修复：Input 与玩法同程序集，测试显式引用 `ProductionChain`

---

## 3. 待改进

- Prefab 组件须 **Inspector 添加**，禁止手改 YAML fileID（N1 Producer 曾溢出 int64）
- IMP 交付前须 L0 模板复述 + EditMode 实测（batchmode 或 Test Runner）
- 手改 asmdef 后须立即在 Unity 验证编译，再声称完成

---

## 4. 验证证据

| 验证项 | 命令/步骤 | 结果 |
|--------|-----------|------|
| Singleton `-Strict` | `skill-本地覆盖.md` ps1 | ✅ exit 0（2026-05-29） |
| EditMode | Test Runner → Run All | ⚠️ 待人工（batchmode 因编辑器已打开项目而中止） |
| Play 模式 | 架构设计 §七 IMP-12～14 项 | ⚠️ 待人工勾选 |
| 程序集编译 | Unity Console 无红字 | ⚠️ 待人工确认 |

---

## 5. 后续行动

| 行动 | 负责人 | 截止 |
|------|--------|------|
| IMP-06 创建 `Player.prefab` | 人工（见 README IMP-06 步骤） | 阶段 G |
| IMP-05 `git init` + 分批 commit | 人工 / AI（需 git CLI） | 阶段 G |
| EditMode Run All 贴结果 | 人工 | 关闭 Unity 后 batchmode 或 Test Runner |

---

## 6. Skill / 流程反馈

- **L2 unity.md EditMode asmdef**：应在 IMP-04 起即建立 `ProductionChain`，避免后期测试 CS0246
- **skill-本地覆盖.md**：已增补程序集结构表；后续 Prefab 变更写 editor-handoff 步骤
