// 仓库类型枚举 — 决定仓库只进不出或只出不进
// Warehouse type enum — determines whether warehouse is input-only or output-only
namespace Core
{
    public enum WarehouseType
    {
        Input,
        Output
    }
}
